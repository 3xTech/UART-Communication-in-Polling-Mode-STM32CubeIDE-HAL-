# UART-Communication-in-Polling-Mode-STM32CubeIDE-HAL-
In this project, I used STM32CubeIDE's HAL to implement a simple management console. The user sends a number to the MCU via USART2, which processes the input and returns a response displayed on the terminal.
